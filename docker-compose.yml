services:
  adguard-sync:
    build: .
    container_name: adguard-sync
    env_file: .env
    ports:
      - "8080:8080"
    volumes:
      - ./config:/config
      - ./data:/data
    restart: unless-stopped
